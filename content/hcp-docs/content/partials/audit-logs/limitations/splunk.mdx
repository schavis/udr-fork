You must use a supported HTTP Event Collector (HEC) endpoint. HCP does not support the following types of HEC endpoints on Splunk Cloud:

- Endpoints with custom firewall rules
- Endpoints configured for AWS Govcloud
- Endpoints created in Splunk Cloud free trials