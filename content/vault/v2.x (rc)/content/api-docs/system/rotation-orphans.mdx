---
layout: api
page_title: /sys/rotation/orphans
description: >-
  Use the `/sys/rotation/orphans` endpoints to manage orphaned automated rotation
  entries for plugins that support automated rotation via the rotation manager.
---

# `/sys/rotation/orphans`

@include 'alerts/enterprise-only.mdx'

Use the `/sys/rotation/orphans` endpoints to manage [orphaned](/vault/docs/enterprise/rotation-policies#orphaned-rotation-attempts)
rotation entries for plugins that support automated rotation via the Rotation Manager.

## Read a list of orphans

Fetch a list of all orphaned rotation entries in the rotation manager. We
recommend reviewing orphaned entries for insight into broken/failing rotations.

| Method | Path                           |
| :----- | :----------------------------- |
| `GET`  | `/sys/rotation/orphans/` |

### Sample request

```shell-session
$ curl \
    --header "X-Vault-Token: ${VAULT_TOKEN}" \
    ${VAULT_ADDR}/v1/sys/rotation/orphans
```

### Sample response

```json
{
    "orphans": ["ldap/static-role/mary", "ldap/static-role/alice"]
}
```
