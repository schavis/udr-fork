Make a `POST` call to
[`{mount_path}/library/{set_name}`](/vault/api-docs/secret/ldap#library-set-management)
to apply your `library.json` configuration file:

```shell-session
$ curl                                                \
  --request POST                                      \
  --header    "X-Vault-Token: ${VAULT_TOKEN}"         \
  --namespace "X-Vault-Namespace: ${VAULT_NAMESPACE}" \
  --data @library.json                                \
  ${VAULT_ADDR}/v1/<mount_path>/library/<set_name>
```

For example:

<CodeBlockConfig hideClipboard="true" highlight="5">

```shell-session
$ curl                                                \
  --request POST                                      \
  --header    "X-Vault-Token: ${VAULT_TOKEN}"         \
  --namespace "X-Vault-Namespace: ${VAULT_NAMESPACE}" \
  --data @library.json                                \
  ${VAULT_ADDR}/v1/devcreds/library/accounting-team
```

</CodeBlockConfig>