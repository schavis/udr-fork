<!-- This partial appears on the tfstate/v1 reference page in all versions of Sentinel -->

<Note>

The `tfstate/v1` import is deprecated. To avoid disruptions, use the `tfstate/v2` import instead. 

</Note>